{%- comment -%}
  The editorial / advertorial templates now use the same type as the rest of
  the store, so the two never look like different brands. Kept as its own
  snippet because those templates render with {% layout none %} and load
  their own head.
{%- endcomment -%}
{% render 'pm-fonts' %}
